import requests 

r = requests.get("https://www.google.com.co")
print(r)
#200 quiere decir ok
#http status codes

#ahora vamos a crear un ambiente virtual, por que instalamos el pip de manera local
#python -m venv env
#lib vamos a ver las dependencias instaladascl
